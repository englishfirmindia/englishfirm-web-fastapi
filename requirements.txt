fastapi>=0.100.0
uvicorn[standard]>=0.23.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt<4.0.0
boto3>=1.28.0
python-dotenv>=1.0.0
pytest>=7.4.0
httpx>=0.24.0
pytest-asyncio>=0.21.0
python-multipart>=0.0.9
slowapi>=0.1.9
requests>=2.31.0
openai>=1.40.0
# Pinned to the last-known-good production version (was running in image
# 3125ce3 / task def 199). An unpinned rebuild on 2026-08-22 pulled a newer
# SDK whose messages.create() rejects the `temperature` kwarg, silently
# breaking all SWT/SST Claude grammar scoring. Keep pinned to avoid a repeat.
anthropic==0.117.0
azure-cognitiveservices-speech>=1.40.0
pydub>=0.25.0
pyspellchecker>=0.8.0
stripe>=10.0.0
